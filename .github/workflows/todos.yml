name: TODOs

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2

    - name: todo-actions
      uses: dtinth/todo-actions@v0.2.0
